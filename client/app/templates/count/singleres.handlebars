

<div class="row">
{{selectedEntity}}
<legend><h3 class="text-center">{{data.title}}</h3></legend>

    <div class="col-sm-12">
        <div class="row">

      <div class="col-sm-2">
        <p class="text-right">版本选择</p>
      </div>
      <div class="col-sm-4">
      {{type-ahead url='evaluate-web/rest/evaluate/evaluateVersion' template='' selection=version placeholder='选择不同的版本'}}
      </div>
      <div class="col-sm-2">
        <p class="text-right">资源类别</p>
      </div>
      <div class="col-sm-4">
					{{view Ember.Select id="entitys" 
					  contentBinding="content"
					  selectionBinding="selectedEntityEnum"  
					  optionValuePath="content.key"  
					  optionLabelPath="content.value" 
					   }}
     </div>

    </div>
<p>   </p>
    <!--雷达图-->
        {{vbar-chart height=400 content = data}}
        
       <p>   </p>
		<p>详细信息</p> 
      <table class="table table-hover">

                <thead>
                <tr>
	              <th>序号</th>
			      <th>维度</th>
			      <th>分值</th>
                </tr>
                </thead>
                <tbody>
                  {{#each item in data.items}}
                  <form class="form-horizontal">
                  
                       <tr>
                          
                          <td>{{item.index}}</td>
					      <td>{{item.dimension}}</td>
					      <td>{{item.score}}</td>

                      </tr>
                      </from>
                  {{/each}}
                </tbody>
            </table>
    </div>
</div>